# ============================================================================
# Variables de entorno — Dashboard Grupo Effi
# ----------------------------------------------------------------------------
# Copiar este archivo a `.env.local` y rellenar los valores reales.
# `.env.local` está en .gitignore — nunca se commitea.
# ============================================================================

# --- Supabase (compartido con UGC Colombia y proyectos de Alexander Cast) ---
VITE_SUPABASE_URL=https://domxgsrajwyuaffiqbtr.supabase.co
VITE_SUPABASE_ANON_KEY=<pegar anon key del proyecto Supabase>

# --- Config general ---
VITE_APP_ENV=development
VITE_APP_NAME=Dashboard Grupo Effi
